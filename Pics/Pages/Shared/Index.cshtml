<!DOCTYPE html>
<html>
<head>
  <script type="text/javascript">
    constants = {
      signIn: '@Url.RouteUrl("DefaultApi", new {controller = "User", action = "SignIn" })',
      signUp: '@Url.RouteUrl("DefaultApi", new {controller = "User", action = "SignUp" })',
      newImage: '@Url.RouteUrl("DefaultApi", new {controller = "Image", action = "ImportImage" })',
      getImages: '@Url.RouteUrl("DefaultApi", new {controller = "Image", action = "GetImages" })',
      removeImage: '@Url.RouteUrl("DefaultApi", new {controller = "Image", action = "RemoveImage" })',
      getPagesCount: '@Url.RouteUrl("DefaultApi", new {controller = "Image", action = "GetPagesCount" })'
    }
  </script>
  <meta name="viewport" content="width=device-width" />
  <title>Pics</title>
  <link rel="stylesheet" href="https://netdna.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" />
  <style>

    a {
      cursor: pointer;
    }

    .help-block {
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div id="app"></div>
  <script type="text/javascript" src='@Url.Content("~/ClientApp/bundle/bundle.js")'></script>
</body>
</html>
